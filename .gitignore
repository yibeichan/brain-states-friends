# uv / Python
.venv/
__pycache__/
*.pyc

# marimo notebook cache (generated by figure scripts)
__marimo__/

# local machine config (paths) — never commit
.env

# analysis outputs and session scratch
output/
logs/*
!logs/.gitkeep
.claude/
.superpowers/

# git worktrees checked out inside the repo (embedded repos, never content)
.worktrees/

# plans and specs from tooling; not part of the supplement
docs/superpowers/

# local project-page staging (see main's site-staging rule)
site-staging/
